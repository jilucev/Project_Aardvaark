
/ signup form for users
%h3 Organization sign in/up

= form_for @organizations do |f|

  = f.label "name of organization"
  = f.text_field :name
  %br
  = f.label "name of representative"
  = f.text_field :representative_name
  %br
  = f.label "email"
  = f.text_field :email
  %br
  = f.label "password"
  = f.password_field :password
  %br
  = f.submit "Submit"
  %br

= form_tag session_path do#, :method => :post do 

  = label_tag "email"
  = text_field_tag "email"
  %br
  = label_tag :password
  = password_field_tag :password
  %br
  = submit_tag "Submit"

  %h3= "logout, #{current_user.name}"

  = form_tag session_path do

    = submit_tag "Logout"

